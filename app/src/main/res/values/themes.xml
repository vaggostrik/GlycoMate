<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.GlycoMate" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Change background to white to match your splash -->
        <item name="android:windowBackground">#FFFFFF</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Pure white background for the system transition -->
        <item name="windowSplashScreenBackground">#FFFFFF</item>
        <!-- Use a transparent icon so we don't see the "shrunk" version -->
        <item name="windowSplashScreenAnimatedIcon">@android:color/transparent</item>
        <item name="postSplashScreenTheme">@style/Theme.GlycoMate</item>
    </style>
</resources>
